#!/bin/sh

if [ "$1" == "incr" ];then
	brightnessctl s 5%+
elif [ "$1" == "decr" ];then
	brightnessctl s 5%-
fi

current_brightness=$(brightnessctl g)
max_brightness=$(brightnessctl m)

dunstify -t 1000 -r 25 -h int:value:$(echo $(($current_brightness*100/$max_brightness))) 'Brightness'
