#!/bin/sh

col=$(colorpicker --short --one-shot --preview)
echo "$col"
echo "$col" |tr -d '\n' | xclip -selection clipboard; dunstify -r 177013 -t 3000 " Color Picker" "<span foreground='$col'><b>$col</b></span> Copied to clipboard";
